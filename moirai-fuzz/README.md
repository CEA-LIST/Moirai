# Fuzzer

The fuzzer tool generates random partial order of updates to a (possibly composite) replicated data type. The fuzzer can be used to test the correctness of the replication protocol implementation, as well as the correctness of the Pure CRDT semantics.
